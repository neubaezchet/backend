    Backend FastAPI (Multiempresa ready)

Run locally:
- pip install -r requirements.txt
- uvicorn app.main:app --reload --port 8000

Deploy: use Render or Railway. Use DATABASE_URL env var for Postgres.
